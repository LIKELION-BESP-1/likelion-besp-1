<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.besp.likebesp1.board.repository.BoardRepository">
    <select id="Board_getList"  resultType="BoardDto" parameterType="long">
        select
            A.boardId
             ,A.boardName
             ,A.createdDate
        from Board A
    </select>

    <select id="Board_getView"  resultType="BoardDto" parameterType="long">
        select
            A.boardId
             ,A.boardName
             ,A.createdDate
        from Board A
        where A.boardId = #{boardId}
    </select>

    <insert id="Board_insert">
        <selectKey resultType="Integer" keyProperty="boardId" order="BEFORE">
            select nvl(max(boardId), 0)+1 from Board
        </selectKey>
        insert into Board (boardId, boardName, createdDate)
        values (#{boardId}, #{boardName}, sysdate)
    </insert>

    <select id="Board_getTotalPosts" resultType="int" parameterType="BoardDto">
        select count(*) from Board
        where 1=1
        <if test="boardName != null">
            and boardName like '%' || #{boardName} || '%'
        </if>
    </select>

    <select id="Board_getPagedList" resultType="BoardDto" parameterType="java.util.Map">
        select *
        from (
        select rownum as rn, b.*
        from (
        select A.boardId, A.boardName, A.createdDate
        from Board A
        where 1=1
        <if test="dto.boardName != null">
            and A.boardName like '%' || #{dto.boardName} || '%'
        </if>
        order by A.createdDate desc
        ) b
        where rownum &lt;= #{endIndex}
        )
        where rn &gt; #{startIndex}
    </select>
</mapper>